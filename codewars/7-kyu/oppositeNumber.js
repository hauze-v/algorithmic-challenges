/*
  Very simple, given a number, find its opposite
*/

// function opposite(number) {
//   if (number > 0 ) {
//     return (-1 * number);
//   } else if (number < 0) {
//     return Math.abs(number);
//   } else {
//     return number;
//   }
// }

// Or more simply:

function opposite(number) {
  return -number;
}